-- Placeholder migration for Ontario Tax Calculator
-- Replace with your actual schema (e.g. tax brackets, config tables)
-- Example:
-- CREATE TABLE IF NOT EXISTS tax_brackets (
--   id SERIAL PRIMARY KEY,
--   year INT NOT NULL,
--   bracket_min DECIMAL NOT NULL,
--   bracket_max DECIMAL,
--   rate DECIMAL NOT NULL
-- );

SELECT 1;
